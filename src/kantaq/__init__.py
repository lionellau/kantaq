"""kantaq — local-first, agent-native issue tracker.

This is the workspace umbrella package. It carries the version and the
``kantaq`` command-line entrypoint (see :mod:`kantaq.cli`). Feature code lives
in the workspace members under ``packages/`` and ``apps/``.
"""

__version__: str = "0.0.5"
