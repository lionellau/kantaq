# ADR-0001 — Tech stack and repository tooling

**Status:** Accepted (v0.0.5) · **Epic:** E01 · **Module:** MOD-01
**Context source:** the project's architecture reference and dev plan
(maintained by the core team separately).

## Decision

kantaq is a **hybrid Python-core + TypeScript-UI** application:

| Concern | Choice | Why |
|---|---|---|
| Core (protocol, sync, MCP gateway, domain, CLI) | **Python 3.12**, **FastAPI** runtime | One language for the security-critical core; FastAPI gives OpenAPI for free. |
| MCP server | official **Python MCP SDK**, HTTP transport (stdio in v0.3) | First-party SDK; loopback HTTP is the lowest common denominator. |
| Local database | **SQLite (WAL)** with **SQLModel** + **Alembic** | Zero-setup local replica; one model definition, two dialects. |
| Crypto | **PyNaCl** (Ed25519), golden vectors checked in | Battle-tested libsodium binding. |
| Web UI | **React + Vite** (TypeScript), built static, served by FastAPI | One process, one port (3939), one token. |
| Layer contract | **OpenAPI** generated by FastAPI; the TS client is regenerated in CI | No hand-written client drift. |
| Env & packages | **uv** (+venv) for Python, **pnpm** for `web/` | Fast, reproducible, lockfile-based. |
| Tests & lint | **pytest, ruff, mypy** (Python); **Vitest, Biome** (web) | Fast, single-binary linters. |

The runtime serves the built UI as static files, so a member runs **one process on
one port** (default `3939`) with one token.

## Repository layout

See the [README](../README.md#repository-layout). A `uv` workspace rooted at the
repo top; each member under `packages/*` and `apps/*` is its own package with a
`src/` layout. The umbrella `kantaq` package carries the version and the `kantaq`
CLI, and depends on every member so a single `uv sync` installs the whole workspace.

## Golden-rule reuse decisions (E01 tooling)

Per the Golden rule (reuse before build), the bootstrap tooling below was chosen
over building anything from scratch. All clear the license bar (no GPL/AGPL) and
the maintenance/credibility bar.

| Need | Chosen | License | Notes |
|---|---|---|---|
| Python env + workspace | **uv** (Astral) | Apache-2.0 / MIT | Fast resolver, native workspaces, built-in lockfile. |
| Python lint + format | **ruff** (Astral) | MIT | Replaces flake8/isort/black; single binary. |
| Python type checker | **mypy** | MIT | Strict mode on the core. |
| Python tests | **pytest** | MIT | De-facto standard. |
| Web runtime API | **FastAPI** + **Starlette** + **uvicorn** | MIT / BSD | OpenAPI-native; async; widely used. |
| Web build | **Vite** + **React** | MIT | Fast dev server + static build. |
| Web tests | **Vitest** | MIT | Vite-native test runner. |
| Web lint + format | **Biome** | MIT / Apache-2.0 | Single fast binary for lint + format. |
| Web routing (E18) | **react-router-dom** | MIT | SPA routing for the 5-page shell; React Router team, ~54k stars. |
| Runtime config (E22) | **pydantic-settings** | MIT | `.env` loading + validation; pydantic org; pairs with FastAPI/pydantic already in the stack. |
| Property tests (E27) | **hypothesis** | MPL-2.0 | Property-based tests for the harness + sync profiles; HypothesisWorks, ~7.5k stars. MPL-2.0 is reuse-compatible. |
| Test fakes (E27) | **built from scratch** (stdlib) | Apache-2.0 (ours) | `FakeClock`/`SeededRandom`/`FakeBackend` are tiny + injectable. freezegun (<5k stars) globally monkeypatches time — heavier and less explicit than an injected clock. |
| CLI | **argparse** (stdlib) | PSF | Zero new dependency for the small bootstrap CLI; revisit (Typer/Click) when the CLI grows. |
| Conventional-commit lint | **conventional-pre-commit** | MIT | commit-msg hook. |

When a feature module starts, run its own Golden-rule pass for the libraries that
module needs (PyNaCl, SQLModel, Alembic, the MCP SDK, the OpenAPI→TS generator,
etc.) and record the choice here.

### E02 / MOD-02 data layer

| Need | Chosen | License | Notes |
|---|---|---|---|
| ORM + schema-as-code | **SQLModel** | MIT | Pydantic + SQLAlchemy in one model; FastAPI author; ~16k stars; shares Pydantic with the rest of the stack. Beat **raw SQLAlchemy** (more boilerplate, no Pydantic bridge) and **Tortoise/Peewee** (own migration story, weaker typing). Architecturally mandated by ADR-0001. |
| Migrations | **Alembic** | MIT | The canonical migration tool for SQLAlchemy/SQLModel, by the SQLAlchemy maintainers — the only option that diffs against `SQLModel.metadata` (`compare_metadata`). Below the 5k-star bar in isolation, but it ships as part of the SQLAlchemy ecosystem (credible org, no advisory) and has no real alternative for this stack; **yoyo-migrations** and **migra** don't read SQLAlchemy metadata. Mandated by ADR-0001. |
| ULID ids | **built from scratch** (`kantaq_db.ids`, stdlib) | Apache-2.0 (ours) | Architecture §6 requires ULID ids. No ULID library clears the 5k-star bar (`python-ulid` ~0.6k, `ulid-py` ~0.5k), so we implement the spec directly: 48-bit ms timestamp + 80-bit randomness, Crockford Base32, monotonic factory. ~90 lines + tests. We implement the real spec (sortable, timestamp-recoverable) rather than claim a standard we don't honor — the test harness made the same call when it renamed its look-alike to `sortable_id`. |
| Postgres driver (tests) | **psycopg** (v3) | LGPL-2.1 (driver, dynamically linked) | Dev/test-only, for the live SQLite/Postgres parity check; not shipped in the runtime path. Standard Postgres driver, psycopg org. |

### E06 / MOD-06 identity & grants

| Need | Chosen | License | Notes |
|---|---|---|---|
| Token hashing (Argon2id) | **cryptography** (pyca) | Apache-2.0 / BSD | ~7k stars, PyCA org, very active, no unpatched advisory. Exposes **Argon2id** (RFC 9106) with PHC-encoded hashes since v45; official wheels bundle OpenSSL ≥ 3.2 so it works everywhere uv installs it. Beat **argon2-cffi** (~0.7k stars — the canonical binding but below the 5k bar), **pyca/bcrypt** (~1.3k stars, below bar; bcrypt also caps input at 72 bytes), and **passlib** (unmaintained since 2020). Params: m=64 MiB, t=3, p=4 (RFC 9106 §4, second recommended profile). |
| Token entropy + lookup | **secrets** (stdlib) + keyed format | PSF / ours | `kq_<token_ulid>.<secret>` — the row id travels in the token (GitHub/Stripe pattern) so verification is one lookup + one Argon2id check, not O(active tokens). |
| OS keychain | **built from scratch** (`FileKeychain`, 0600 file) | Apache-2.0 (ours) | No Python keychain library clears the bar: **keyring** (~1.3k stars), **secretstorage** (Linux-only, <1k), **pyobjc keychain** (macOS-only). v0.0.5 parks the runtime token in a 0600 file beside the SQLite replica; the real OS-keychain backend is re-evaluated with the v0.1 device keys (D-01), which actually need it. The `Keychain` protocol keeps callers indifferent; MOD-30 ships `FakeKeychain` against the same contract test. |

### E29 / MOD-16 docs & distribution

| Need | Chosen | License | Notes |
|---|---|---|---|
| Doc link checking | **built from scratch** (~50-line pytest gate, stdlib `re`/`pathlib`) | Apache-2.0 (ours) | The Docs profile needs *internal* links checked hermetically in CI. **lychee** (~12k stars, MIT/Apache) clears the bar but is a Rust CLI aimed at live-URL checking — adds a toolchain + network flakiness for the part we don't want. **linkchecker** is GPL-3.0 (license bar). **markdown-link-check** (~?<5k stars, npm) is below the bar and network-first. A repo-relative resolver inside pytest is deterministic, offline, and rides the existing gate. Revisit lychee at v0.1 launch for external-URL spot checks. |
| Quickstart drift | **built from scratch** (pytest introspects the Makefile + `argparse` parser) | Apache-2.0 (ours) | The fresh-clone CI job *executes* the core path; this gate keeps every documented `make`/`kantaq` command real. No third-party category exists for this. |

**E29-T2a (full-loop finalize, 2026-06-12):** no new capability category — the
task finalizes prose for the now-shipped hero loop (sync + MCP + UI). Golden-rule
re-affirmed: the existing stdlib doc gates (link resolution, command drift) stay
the right tools; nothing external clears the bar for *hermetic internal* checks.
lychee for external-URL spot checks remains the v0.1-launch revisit.

**E29-T2 (v0.1 launch docs, 2026-06-14) — the lychee revisit, now resolved.** With
external-facing docs published (protocol/security/mcp/compatibility + the README +
wedge post), the launch wants the *external* URLs spot-checked. Candidates for
external-URL link checking: **lychee** (~17k★, MIT/Apache-2.0, Rust, actively
maintained, async, handles redirects/fragments/mailto — clears the bar);
**muffet** (~2.8k★, MIT, Go — below the star bar); **markdown-link-check** (npm,
<5k★, network-first — below the bar); **linkchecker** (GPL-3.0 — license bar).
**Decision:** keep CI **hermetic** (the stdlib internal-link gate stays the only
link gate that blocks a PR — network checks are flaky and would feed RISK-05 gate
fatigue), and add lychee behind an **opt-in `make linkcheck`** for release-time
external-URL spot checks. lychee is a *system tool the releaser installs*, not a
repo dependency — it never enters `pyproject`/`uv.lock`, so the fresh-clone gate is
untouched. So: internal links → built-from-scratch hermetic gate (blocking);
external links → lychee, opt-in, manual at release.

### E24 / MOD-05 Supabase backend

| Need | Chosen | License | Notes |
|---|---|---|---|
| Supabase API client (Auth) | **built from scratch on httpx** (`kantaq_backend_supabase.auth`, ~150 lines) | Apache-2.0 (ours) | The official Python stack all fails the 5k-star bar: **supabase-py** ~2.5k★, **supabase/auth-py** ~0.1k★, **supabase/postgrest-py** ~0.3k★ (verified via the GitHub API, 2026-06). v0.0.5 needs five GoTrue REST calls (otp / verify / refresh-token / user / logout, all documented), so a thin typed client over **httpx** (15k★, BSD-3, already in the stack) beats adopting a below-bar SDK plus its transitive pins. Revisit supabase-py at v0.2 (atomic RPC + Realtime). |
| JWT claim parsing (key guard) | **PyJWT** | MIT | ~5.7k★, active, the focused RFC 7519 implementation. Beat **python-jose** (~1.8k★, below bar, history of advisories) and **Authlib** (~5.3k★ — clears the bar but is a full OAuth/OIDC framework; we only read claims). Used unverified-decode only: the client has no signing secret by design, the guard is a local misconfiguration check (refuse a `service_role` key), and Supabase verifies keys server-side. |
| RLS test infrastructure | **EphemeralPostgres** (MOD-30, kept) + a Supabase auth-env stub + `TamperedClient` (built, stdlib + SQLAlchemy) | Apache-2.0 (ours) | Resolves the MOD-30 open question (testcontainers vs disposable local Postgres): **testcontainers-python** is ~2.2k★ (below bar) and adds a Docker-API dependency; **pytest-postgresql** is below bar. The existing EphemeralPostgres (disposable database on a provided server, CI Postgres service) already proved itself in E02; E24 adds a faithful stub of Supabase's documented auth environment (`anon`/`authenticated`/`service_role` roles, `auth.jwt()`/`auth.uid()` reading `request.jwt.claims`) so the *checked-in* policies are tested against the contract they run under in production. |
| Authorization model | **PostgreSQL Row Level Security** (policies in `supabase/policies/`) | n/a (SQL standard feature) | The real, server-enforced standard — no homegrown authz DSL. Policies follow Supabase's published conventions (SECURITY DEFINER helper functions against RLS recursion; per-verb policies; `service_role` with BYPASSRLS staying backend-side). |
| Sync endpoints (E24-T4, Sprint 2) | **PostgREST's documented REST dialect** (Supabase's data API), thin client on **httpx** (`kantaq_backend_supabase.sync`, ~200 lines) | PostgREST MIT (server, run by Supabase); ours Apache-2.0 | Golden-rule re-run 2026-06 (GitHub API): **supabase-py** 2,518★ and **supabase/postgrest-py** 317★ stay below the bar; **PostgREST** itself (27,220★, MIT, active) *is* the recognized standard here — Supabase runs it, so v0.0.5 needs zero server code. We implement its documented dialect properly: bulk insert with `on_conflict` + `Prefer: resolution=ignore-duplicates` (= Postgres `ON CONFLICT DO NOTHING`, the idempotent-re-push contract), `return=representation` for commit-order revisions from the identity column, `gt./eq.` filters + `order`/`limit` paging. Realtime stays out (polling is the v0.0.5 baseline; revisit with v0.2's RPC). Tested against the dialect for real: MOD-30's `FakePostgREST` answers the same requests with real SQL + RLS on a disposable Postgres. |
| Atomic commit RPC (E24-T6, v0.2, D-09) | **plpgsql function exposed via PostgREST RPC** (`supabase/rpc/events.sql`) — build-on-the-adopted-standard | PostgREST MIT (server); ours Apache-2.0 | Three candidates for the v0.2 atomic commit: (1) a **PostgREST RPC** (the already-chosen standard — `POST /rest/v1/rpc/<fn>` over a plpgsql function, zero new dependency, one transaction with a per-workspace advisory xact lock that closes the commit-visibility window); (2) a **Supabase Edge Function** (Deno + supabase-js — an extra deploy surface, cold starts, and a second language for the commit primitive); (3) **pgsodium** for server-side Ed25519 — rejected: it is *pending deprecation* (Supabase docs, 2026-06) and is a Postgres extension absent from the stock CI / self-host Postgres, so it fails the "fits the stack" bar and would make the live test suite un-runnable. Chosen: the plpgsql RPC. Consequence (honest-naming, MOD-17): stock Postgres has no Ed25519, so the RPC verifies grant **state** + ordering + signature **presence** atomically against committed state and the Ed25519 **byte** check stays on the client/edge `VerifyingBackend` (already on `cryptography`/PyNaCl). A pgsodium-capable or Edge deployment could add the byte-check later without changing the contract. |
| Append-only enforcement past BYPASSRLS (E24-T7, v0.2) | **PostgreSQL triggers** (`BEFORE UPDATE OR DELETE` row + `BEFORE TRUNCATE` statement) | n/a (SQL standard feature) | RLS withholds UPDATE/DELETE from client roles, but `service_role` carries BYPASSRLS + `grant all`, so RLS cannot bind it. A `REVOKE` is incomplete (the table owner and BYPASSRLS still reach it) and TRUNCATE is a separate privilege. The standard mechanism that fires for **every** role — owner and BYPASSRLS included — is a trigger; two are needed (row-level for UPDATE/DELETE incl. `ON CONFLICT DO UPDATE`, statement-level for TRUNCATE). No homegrown mechanism, no new dependency. |

**Verification cost vs the 5 s revocation budget (NFR-E06-2).** Argon2id is
deliberately slow (~50 ms), too slow for every request, so the runtime caches
verified tokens in memory for **3 s** (< 5 s): a revoked token keeps working at
most TTL seconds across processes, and same-process revocation purges the cache
immediately. The cache stores a SHA-256 digest of the presented token, never the
token itself.

**D-07 (single-source dialect generation) — resolved as "shared SQLModel."** The
8 collections are defined once as SQLModel models; both SQLite and Postgres DDL
are generated from that one `MetaData` via SQLAlchemy's dialect compilers (no
hand-kept second schema). Parity is proven two ways: an offline structural
fingerprint that asserts both dialects describe the same schema (hermetic, runs
everywhere) and a live `EphemeralPostgres` reflection diff in CI. To keep the
dialects in lock-step we use only portable column types — `str`/VARCHAR for
enum-like fields (no native Postgres `ENUM`) and generic `JSON` for list/dict
fields. The fallback in the sprint risk note (two hand-kept schemas + a CI diff)
was not needed.

### E04 / MOD-04 sync engine

| Need | Chosen | License | Notes |
|---|---|---|---|
| Event-log sync / replication | **built from scratch** (`kantaq_sync_engine`, on SQLModel + SQLAlchemy already in the stack) | Apache-2.0 (ours) | The append-only event log with per-actor `actor_seq`, LWW by server commit order, and propose-first semantics *is* kantaq's protocol (PRD §13) — adopting a sync product would replace the product. Candidates evaluated: **ElectricSQL** (~9k★, Apache-2.0 — clears the bar but is Postgres-to-client partial replication with its own write model, not an application event protocol); **Yjs** (~20k★, MIT) and **cr-sqlite** (~3.5k★) are CRDTs — explicitly out of scope for MVP (architecture §10 "No CRDT in MVP"); **Litestream** (~12k★, Apache-2.0) replicates the SQLite *file*, not per-actor events. Storage/ORM reuse stays SQLModel/Alembic (ADR-0001). |
| Snapshot encoding | **NDJSON (JSON Lines convention)** via stdlib `json` | n/a (convention) | One JSON object per line, entities sorted by id, keys sorted, compact separators — deterministic byte-for-byte, so replica equality is string equality and the v0.2 export bundle diffs cleanly. No library needed beyond the stdlib. |
| Convergence property testing | **hypothesis** (already adopted, E27) | MPL-2.0 | Drives random two-replica interleavings against the MOD-30 FakeBackend + two-replica simulator. |

### E18 / MOD-10 web foundation (typed client)

| Need | Chosen | License | Notes |
|---|---|---|---|
| OpenAPI → TS types | **openapi-typescript** | MIT | openapi-ts org, ~10k★, active. Generates *types only* from the checked-in `web/src/api/openapi.json` — no runtime codegen to drift. Beat **openapi-generator** (~24k★, Apache-2.0, but drags a Java toolchain into web CI) and **orval** (~4.5k★) / **swagger-typescript-api** (~3.6k★), both below the 5k bar. |
| Typed fetch client | **openapi-fetch** | MIT | Same org/repo as openapi-typescript; ~6 kB wrapper that types paths, params, and payloads from the generated `paths`. Middleware hook carries the session bearer token and the 401→disconnect rule. |
| Contract drift gate (D-08) | **built from scratch** (one pytest + one CI step) | Apache-2.0 (ours) | py CI: `test_openapi_drift` regenerates the document byte-for-byte (same pattern as the Supabase DDL gate). web CI: `pnpm gen:api && git diff --exit-code`. Each toolchain gates its own half; no cross-toolchain CI dependency. |

### E12 / MOD-03 tracker domain

| Need | Chosen | License | Notes |
|---|---|---|---|
| CRUD + validation layer | **FastAPI + Pydantic v2 + SQLModel** (already in the stack) | MIT | The domain service is a thin layer over the ADR-0001 stack (FastAPI ~80k★, Pydantic ~22k★, SQLModel ~16k★ — all active, MIT). CRUD *generators* on top were evaluated and all fail the 5k bar: **fastapi-crudrouter** (~1.4k★, dormant since 2023), **fastcrud** (~1.5k★), **SQLAdmin** (~2.1k★, an admin UI not an API). The tracker rules (every mutation → audit row + DomainEvent, propose-first fields later) are bespoke anyway, so generated CRUD would be rewritten immediately. |
| Multipart upload parsing | **python-multipart** | MIT | The parser FastAPI itself requires for `UploadFile` (Starlette ecosystem, maintained by the FastAPI/Starlette org). Adopted as the standard companion dependency rather than hand-parsing multipart. |
| Attachment blob store | **built from scratch** (`kantaq_core.tracker.blobs`, stdlib `hashlib`/`pathlib`, ~100 lines) | Apache-2.0 (ours) | D-13 solo mode is "local filesystem next to the database"; the team path (Supabase Storage) lands with the sync blob endpoint. Generic object-storage libraries (**boto3/MinIO SDK** — server-targeted; **fsspec** ~1.1k★ below bar) solve a different problem. Content addressing is SHA-256 (FIPS 180-4 via stdlib), which also gives the FR-E04-5 hash-verify for free. |
| Typed ticket relationships + integrity (E12-T3) | **built on the in-stack components** (a `ticket_relationships` SQLModel collection + stdlib BFS for acyclicity) | Apache-2.0 (ours) | Golden-rule run 2026-06. The capability is "a typed edge table between tickets with no-self / no-duplicate / no-cycle integrity" — i.e. a small directed graph persisted as a protocol collection. Graph libraries evaluated and rejected as the *wrong shape*: **NetworkX** (~17k★, BSD-3, active — clears the bar, but an in-memory analysis library; the edges must be a persistent SQLModel collection that folds from sync events, and v0.1 needs one acyclicity check over a handful of arcs, not a graph engine — same reasoning recorded for E13/MOD-19), **rustworkx** (~1.3k★, Apache-2.0 — below the star bar), **python-igraph** (~1.6k★ **and** GPL-2.0 — below the bar and a forbidden license). A dedicated graph store would also break D-07 one-model-two-dialects and cannot ride event-fold sync. So the edge is a relational join table (mirroring the E13 `memory_links` precedent) and the cycle guard is ~15 lines of stdlib `deque` BFS over the `blocks`/`causes` families. No standard claimed for relation taxonomies (none exists); the five types are spec'd in MOD-03. |

### E07 / MOD-07 audit

| Need | Chosen | License | Notes |
|---|---|---|---|
| Append-only audit trail | **built from scratch** (`kantaq_core.audit`, on SQLModel + SQLAlchemy events) | Apache-2.0 (ours) | Candidates fail the bar: **SQLAlchemy-Continuum** (~1.7k stars) and **sqlalchemy-history** are versioning add-ons below 5k; **django-auditlog** (~3.5k stars) is Django-only; **pgaudit / postgresql-audit** are Postgres-trigger-based, but kantaq needs SQLite parity and NFR-E07-1 demands *app-layer* enforcement. The build is small: one `write()` path into the existing `audit_events` collection (MOD-02) plus SQLAlchemy's built-in event hooks to refuse mutation at three depths: mapper `before_update`/`before_delete` (unit of work), session `do_orm_execute` (bulk ORM statements), and an engine-level `before_execute` backstop (legacy bulk APIs, table-targeted and bare-connection statements). Hardened per the SEC second review. Zero new dependencies. |
| Tamper-evident hash chain (E07-T3, FR-E07-4) | **built from scratch** (`kantaq_protocol.hashing` on stdlib `hashlib` SHA-256, chained inside `kantaq_core.audit`) | Apache-2.0 (ours) | Golden-rule run 2026-06. The capability is a **hash-linked, tamper-evident log** (Haber-Stornetta time-stamping / Schneier-Kelsey "Secure Audit Logs", the precursor to RFC 6962 — whose Merkle anchoring is the deferred E07 v0.2 step). No reuse-bar library fits: append-only/audit libs were already rejected above; transparency-log toolkits (**transparency-dev/trillian** ~3k★ **and** Go, **sigstore/rekor** ~Go service) are servers in another language, not a Python primitive; chained-hash helpers (**python-blockchain** toys, **hashlog**) are <1k★. The construction is ~20 lines: `SHA-256(DOMAIN ‖ previous_hex ‖ 0x00 ‖ canonical(record))`, where `canonical(...)` is the **one** RFC 8785 codec every signature already uses (no forked encoding — the named E03/E04 risk) and `DOMAIN = kantaq:audit-chain:v1` separates it from event/grant signing the way `EVENT_SIGNING_DOMAIN`/`GRANT_SIGNING_DOMAIN` do. SHA-256 is FIPS 180-4 via the stdlib (same primitive as the FR-E04-5 blob hash). Zero new dependencies. |

### E09–E10 / MOD-08–09 MCP gateway & tools

| Need | Chosen | License | Notes |
|---|---|---|---|
| MCP server + client | **modelcontextprotocol/python-sdk** (`mcp`) | MIT | Golden-rule run 2026-06 (GitHub API): the official SDK, **23.3k★**, pushed within 24 h, Anthropic-backed MCP org, no unpatched advisory. We use the **low-level `Server`** (the gateway needs per-session tool catalogs, custom checks, and audit interception — not a decorator framework) over **streamable HTTP** with the SDK's own `TransportSecuritySettings` (DNS-rebinding/Host validation) — the standard applied properly, not re-implemented. Beat **PrefectHQ/fastmcp 2.x** (25.6k★, Apache-2.0 — clears the bar but is a remote-deployment framework: auth providers, proxying, server composition; far more surface than a loopback security gateway wants, and it wraps these same protocol types) and **tadata-org/fastapi_mcp** (11.9k★, MIT — auto-exposes FastAPI routes as tools, the wrong shape for a capability-scoped gateway; last push 2025-11, fails the ~3-month maintenance bar). Also mandated by ADR-0001 ("official Python MCP SDK"). The harness's FakeMCPClient drives the **same SDK's client** over in-process ASGI, so the fake honors the real contract by construction. |
| Untrusted-content tagging | **built from scratch** (`kantaq_mcp.security`, stdlib `re`, ~30 lines) | Apache-2.0 (ours) | PRD §15.1 content fencing with provenance + marker-escape neutralization. No OSS category exists for this contract (prompt-injection "guard" libraries are model-side classifiers, below the bar and out of scope — the product does not run models). The injection corpus in MOD-30 pins it in CI. |

### E19–E21 / MOD-11–13 tracker, approval, and onboarding UI

| Need | Chosen | License | Notes |
|---|---|---|---|
| Markdown rendering (ticket bodies, comments) | **react-markdown** | MIT | Golden-rule run 2026-06: ~14.9k★, unified collective (remark/rehype maintainers), active, no unpatched advisory. Decisive property for PRD §15: it renders untrusted markdown **to React elements with no `innerHTML`**, and raw HTML in the source is dropped unless `rehype-raw` is added (we do not add it) — a `<script>` in a ticket body stays inert, pinned by a Vitest case. Beat **marked** (~35k★) and **markdown-it** (~20k★): both emit HTML strings, forcing `dangerouslySetInnerHTML` + a sanitizer (DOMPurify) — a bigger, riskier surface for the same job. |
| Server state / data fetching | **none (openapi-fetch + the MOD-14 2 s poll)** | — | Evaluated **TanStack Query** (~46k★, MIT), **SWR** (~32k★, MIT), **RTK Query** (Redux Toolkit ~11k★, MIT) — all clear the bar, none adopted. The locked convergence model is "local replica + 2 s poll" (MOD-14, NFR-E04-1): views refetch on one cadence and render what the replica says. A client cache with invalidation semantics would sit *on top of* a local replica that already is the cache, duplicating state for ~5 screens. Revisit if v0.1 adds realtime push. |
| Hero-flow end-to-end runner | **Playwright** (`@playwright/test`) | Apache-2.0 | Named by the harness standard (§4, UI profile) and now adopted in code: ~76k★, Microsoft, weekly releases. Beat **Cypress** (~49k★, MIT — slower runs, no first-class multi-context, bundled service upsell) and **Selenium/WebDriverIO** (lower-level, no built-in `webServer`/trace tooling). The `webServer` hook boots the real runtime (`scripts/e2e_server.py`), so the e2e drives the same stack a member runs. |

### E13 / MOD-19 memory spaces & graph

| Need | Chosen | License | Notes |
|---|---|---|---|
| Note/graph data model (memory entries + ticket↔memory links) | **built on the in-stack components** (SQLModel + Alembic, two protocol collections) | Apache-2.0 (ours) | Golden-rule run 2026-06. Evaluated **NetworkX** (~17k★, BSD-3, active — clears the bar, but it is an in-memory graph *analysis* library; kantaq's memory graph must be persistent protocol collections that fold from sync events, and v0.1 needs zero graph algorithms over a bipartite ticket↔memory link table), **Kùzu** (MIT embedded property-graph DB — **archived 2025-10** after the Apple acquisition, fails the maintenance bar; a second storage engine beside SQLite would also break D-07 one-model-two-dialects and cannot participate in event-fold sync), and **RDFLib** (~4.7k★, BSD-3 — below the 5k bar; RDF triples don't map to SQLModel collections). At v0.1 scale the "graph" is a relational join table (`memory_links`) on the already-mandated SQLModel/Alembic/SQLite stack — the only shape the sync protocol can carry. Provenance: considered W3C PROV-DM as a vocabulary; deliberately **not** claimed — we do not implement its entity/activity/agent model. Memory provenance is a small structured dict (`origin`, `actor_id`, `captured_at`, `detail`), spec'd in MOD-19. |
| Promotion state machine (E13-T4, v0.2) | **built on the in-stack `proposals` CAS pattern** (`MemoryService.promote/approve/reject`) | Apache-2.0 (ours) | Golden-rule run 2026-06. The transition `draft→proposed→approved` is a 3-state human-gated workflow. Evaluated **pytransitions/transitions** (~6.1k★, MIT — *clears* the star/maintenance bar) and **python-statemachine** (~1.2k★, below bar). But the FSM mechanics are the trivial part; the value (and the SEC surface) is the **compare-and-swap status flip + the agent-cannot-approve permission split + the attributed audit + the signed `_emit_team_only`** — exactly what the already-shipped `kantaq_core.proposals` pattern provides and what a generic FSM library does NOT. Adopting `transitions` would add a dependency for the easy 5% and still leave us hand-writing the hard 95%, and would diverge from the proposals precedent reviewers already know. So: build on the in-stack pattern (a conditional `UPDATE … WHERE review_status='proposed'`, mirroring `proposals._flip_status`). No new dependency. |

### E28 / MOD-25 telemetry

| Need | Chosen | License | Notes |
|---|---|---|---|
| Opt-in usage telemetry (local-only, D-10) | **built from scratch** (`kantaq_core.telemetry`, ~200 lines on the existing SQLModel stack) | Apache-2.0 (ours) | Golden-rule run 2026-06: **opentelemetry-python** (~2k★ SDK — below the 5k bar, and its exporter/collector architecture is the exact thing D-10 forbids at MVP), **posthog-python** (SaaS-first SDK; "no third-party analytics" is in MOD-25's scope line), **structlog** (~4.2k★ — below bar, and it ships logs, not queryable outcome metrics). Nothing fits a *local-only, content-forbidden, SQLite-resident* event table better than one table + a typed registry: every event name declares its allowed prop keys/types (numeric/categorical only), so recording ticket/memory content is a `TelemetryError`, not a code-review hope. Revisit a collector post-v0.2 (D-10). |

### E03 / MOD-17 protocol core

| Need | Chosen | License | Notes |
|---|---|---|---|
| Ed25519 sign/verify (RFC 8032) | **cryptography** (pyca) | Apache-2.0 / BSD | Golden-rule run 2026-06: ~7.6k★, PyCA org, release activity within days, no unpatched advisory — and already in the stack (Argon2id, E06), so the signing path adds zero new runtime dependencies. Beat **PyNaCl** (PyCA libsodium binding — credible and active but ~1.1k★, below the 5k bar) and **python-ed25519** (unmaintained). ADR-0001's "PyNaCl" line is superseded by this pass (the ADR predates the bar being applied to it); **PyNaCl stays in the build as the D-11 second implementation** — a dev-only dependency that cross-verifies every golden vector (OpenSSL vs libsodium, genuinely independent codebases), plus the RFC 8032 §7.1 vectors grounding both. |
| Canonical byte encoding | **built from scratch** (`kantaq_protocol.canonical`, RFC 8785 restricted profile, ~120 lines on stdlib `json`) | Apache-2.0 (ours) | The encoding **is** RFC 8785 (JCS) — UTF-16 code-unit key sort, JCS string escaping, no whitespace — restricted to forbid floats (the cross-language divergence point; the Matrix canonical-JSON precedent) and ints beyond 2^53−1 (the I-JSON bound). No JCS library clears the bar: **trailofbits/rfc8785.py** (~0.1k★), **titusz/jcs** (<0.1k★), **cyberphone reference py3** (sample code). **orjson** (~6.5k★) clears the star bar but sorts keys bytewise (deviates from JCS on non-BMP keys) and would pin a Rust extension to ~120 lines of logic. Golden vectors + hypothesis round-trip properties pin it; `decode` accepts only canonical bytes (re-encode must match), so two spellings of one event cannot coexist under a signature. |
| Capability-grant format | **same canonical codec + Ed25519** (protocol entity, not a token format) | Apache-2.0 (ours) | Evaluated **PyJWT** (~5.4k★, MIT, EdDSA via RFC 8037), **Biscuit** (biscuit-python, tiny), **pymacaroons** (~0.5k★, unmaintained). JWT clears the bar but was rejected on architecture: grants are first-class synced protocol entities (`authoritative_tx` rows with a `sig` column) and the named sprint risk is "one codec or broken signatures" — a base64-JSON token envelope would be a second, parallel signing encoding. The grant signs with the same RFC 8785 bytes as every event. Honest-naming rule applied: we cite RFC 8785/8032, which we implement, and do not claim JWT/UCAN/Biscuit semantics we don't. |

### E06 / MOD-06 device keys & grants (v0.1)

| Need | Chosen | License | Notes |
|---|---|---|---|
| OS keychain for the device seed | **FileKeychain kept** (0600 file, ours) | Apache-2.0 (ours) | The deferred golden-rule re-pass, run at exactly the moment D-01 scheduled it (E06-T4, 2026-06): **jaraco/keyring** is still ~1.5k★ (below the 5k bar; CLI-shaped API, plugin backends of varying quality), **secretstorage** (Linux-only, <1k★), **pyobjc-framework-Security** (macOS-only, binding not an abstraction). Nothing clears the bar, exactly as in v0.0.5 — so the sprint risk note's sanctioned fallback applies: the 0600 ``FileKeychain`` beside the SQLite replica holds the Ed25519 seed; the ``Keychain`` protocol keeps every caller indifferent for a future backend. **DEBT-16 re-pass (2026-06-13, GitHub API):** unchanged — `jaraco/keyring` **1478★** (MIT, actively maintained, pushed 2026-04), `mitya57/secretstorage` **142★** (BSD-3, Linux-only). No cross-platform OS-keychain Python library clears the 5k bar; DEBT-16 is **accepted** as a recorded trade-off (re-check only if a candidate later clears the bar — not a blocker). |
| Grant issuance/verification | **kantaq-protocol (MOD-17) + role matrix** | Apache-2.0 (ours) | No new dependency: grants sign with the E03 codec + pyca/cryptography and derive strictly from the PRD §11 matrix (never widening a role); PyJWT/Biscuit/macaroons were evaluated and rejected in the E03 pass (one codec rule). |

### E14 / MOD-20 lifecycle stages

| Need | Chosen | License | Notes |
|---|---|---|---|
| Lifecycle stage machine | **built from scratch** (`kantaq_core.lifecycle`, stdlib) | Apache-2.0 (ours) | Category: finite-state-machine / workflow library. No candidate clears the bar (checked 2026-06): **pytransitions/transitions** (6.5k★, MIT) fails the maintenance bar — last push 2025-09, ~9 months stale; **python-statemachine** (1.2k★) and **glyph/automat** (0.6k★) are below the star bar; **viewflow** is AGPL-3.0 (license bar). The shape is wrong too: FSM libraries model *enforced* per-instance machines with callbacks, while MOD-20 locks an advisory taxonomy persisted as a ticket column — membership-validated, free transitions, a pure recommend-next rule. One tuple of 9 frozen `Stage` records + two pure functions (~170 lines incl. docs), fully pinned by tests. |

### E20 / MOD-12 Settings tree (v0.1)

| Need | Chosen | License | Notes |
|---|---|---|---|
| Settings navigation tree + section pages | **built on the in-stack React + react-router + `web/src/lib/ui.ts` vocabulary** | Apache-2.0 (ours) | Category: a settings-UI / tree-view / headless-component kit. No candidate fits the shell: **Radix UI** (~15k★, MIT — navigation/accordion primitives) and **shadcn/ui** drag in a component system + a styling approach (Tailwind / CSS-in-JS) the web shell deliberately forgoes (RISK-08: no CSS framework, one inline-style vocabulary); **react-arborist** (~3k★) and **rc-tree** sit around/below the star bar and are the wrong shape (file-tree widgets, not a settings nav); **react-hook-form** (~42k★, MIT) is form-state machinery this read-mostly surface does not need. The tree is a static `react-router` route map rendered through the existing `ui.ts` style objects — identical to every prior web epic (E13/E18/E19/E21). No standard claimed (none exists for a settings IA). |
| Settings interaction / focus polish | **~25-line hand-written base stylesheet** (`web/src/index.css`) | Apache-2.0 (ours) | The taste pass (Leonxlnx/taste-skill, minimalist profile) flagged the states inline styles cannot express — hover / active / `:focus-visible` / transitions. A tiny framework-free base layer adds them at zero specificity (`:where`) via `filter`/`transform`/`outline`, so it never overrides an inline `background`; that is base CSS, not a framework, so RISK-08 holds. DESIGN_VARIANCE and MOTION dials kept deliberately low to match the established editorial-minimal design system. |

### E19-T4 / E20-T3 / MOD-11–12 trust UI — diff + sessions + audit (v0.1)

| Need | Chosen | License | Notes |
|---|---|---|---|
| Proposal **diff** rendering (the new capability) | **built from scratch** (`web/src/components/FieldDiff.tsx`, ~70 lines) | Apache-2.0 (ours) | Golden-rule run 2026-06. The capability is a **structured per-field before→after diff** of a proposal's `diff.changes` (status, priority, assignee, labels, …) against the live ticket — a handful of mostly-short fields, not a text/code-block diff. No renderer clears the bar *in the right shape*: **react-diff-viewer-continued** (~0.8k★) and **react-diff-view** (~1.4k★) are below the 5k bar and are line/word text-diff viewers; **jsondiffpatch** (~4.8k★, below bar) ships an HTML formatter that emits strings needing `dangerouslySetInnerHTML` — forbidden here for the same reason ticket bodies use react-markdown (PRD §15, untrusted agent values must stay inert). **jsdiff** (`diff`, ~14k★, MIT, maintained) *clears* the bar but is a char/word/line **algorithm**, not a renderer, and word-level intra-string highlighting buys little on short fields — evaluated and not adopted (revisit for long-description diffs in v0.2). So the field diff is ~70 lines on the `ui.ts` vocabulary: current value struck through, arrow, proposed value, all plain text. No standard claimed. |
| Cited memory / session / audit-call / tabs rendering | **reused in-stack** (existing `react-router` + `ui.ts` + `react-markdown`; new shared components `CitedMemory`/`SessionCard`/`CallList`/`Tabs`/`ProposalCard`/`MintedToken`) | Apache-2.0 (ours) | No new dependency. Cited memory reuses the ticket's linked-memory endpoint (`GET /v1/tickets/{id}/memory`) — citations resolve at render time (PRD §16.6), not stored on the proposal. The Agents page reuses the signed capability grants (`/v1/agents/sessions`) + the live audit read (`/v1/audit/range`) since the gateway's in-memory sessions live in a separate process (NFR-E20-1's "audit-driven, no cache" follows directly). The tab strip is a ~90-line framework-free `role="tablist"` (RISK-08); a headless-tabs kit (Radix, ~15k★) was rejected on the same grounds as the E20 Settings tree. |

### E16 / MOD-21 context resolver & evals (v0.1 slice)

| Need | Chosen | License | Notes |
|---|---|---|---|
| Memory-read policy engine | **built from scratch** (`kantaq_core.memory_policy`, stdlib) | Apache-2.0 (ours) | Category: authorization / policy engine. No candidate clears the bar *in the right shape* (checked 2026-06): **Open Policy Agent** (Go + Rego; runs as an external PDP/sidecar — the opposite of an in-process, offline, local-first read filter, and Rego is a second policy language to maintain); **Cerbos** (Go PDP service over gRPC/HTTP, same external-service shape); **pycasbin** (~1.8k★, below the star bar — the 18k★ casbin is Go); **vakt** / **py-abac** (ABAC libraries, both well below 5k★, the latter unmaintained). For four hardcoded role policies (`include`/`exclude` over the 7 memory spaces + a `team` privacy floor + a withheld-status set, §6.11) the right shape is the lifecycle pattern: four frozen `MemoryPolicy` records + a pure `filter(entries, policy, now)` (~210 lines incl. docs), fully pinned by tests, **100% branch coverage**. The model is attribute-based access control (subject = agent role; attributes = space/visibility/review_status) applied to memory reads — named honestly, no library or standard claimed. v0.2 makes policies db-backed and user-editable (§8.9); the §12 `memory_policies` table name is reserved for then. |
| Context-eval harness | **built from scratch** (`kantaq_core.evals`, stdlib `json`) | Apache-2.0 (ours) | Category: ML/LLM eval framework. **promptfoo**, **deepeval**, **ragas**, **OpenAI evals** are all LLM-output-grading harnesses (model-call runners, often Node or heavy ML deps) — the wrong shape for grading a deterministic rules-based resolver against checked-in fixtures, and none belongs in the local-first core. The fixture format + loader + validator mirror the in-stack `injection.py` corpus pattern (MOD-30). **E16-T3 (Sprint 4):** precision/recall scoring + a recorded baseline (`evals/baseline.json`) ride on the same loader; `kantaq eval` fails on a drop over 5 points (the FR-E16-5 CI gate). |
| Rules-based context resolver (E16-T2, v0.1) | **built from scratch** (`kantaq_core.context`, stdlib) | Apache-2.0 (ours) | Category: retrieval / context assembler. No OSS clears the bar in shape: RAG frameworks (**LlamaIndex** ~37k★, **LangChain** ~95k★) are model-call retrieval pipelines (embeddings, vector stores, heavy deps) — the resolver is a deterministic rules filter over the local store graded against checked-in fixtures, not a retriever. ~150 lines: gather the ticket's linked ∪ in-scope memory (team-only), run the `memory_policy` filter, shape included / excluded-with-reason / missing + a coarse token estimate. Validated at **precision = recall = 1.0** against the now-complete **100 graded fixtures (80 agent cells, E16-T4b)** — the recorded regression baseline. |

### E17 / MOD-22 recommendations & skills (v0.1)

| Need | Chosen | License | Notes |
|---|---|---|---|
| Recommendation rule engine | **built from scratch** (`kantaq_core.reco`, stdlib) | Apache-2.0 (ours) | Category: business-rules / rule engine. No candidate clears the bar (checked 2026-06): **jruizgit/rules** (durable_rules, 1.3k★), **venmo/business-rules** (989★, last push 2024-08), **zerosteiner/rule-engine** (588★), **noxdafox/clipspy** (CLIPS bindings, 200★) — all well below 5k★, and a RETE/CLIPS forward-chaining engine is the wrong shape for a tiny rule set already keyed on the lifecycle taxonomy. The right shape is the MOD-20/MOD-21 pattern: a frozen `SkillContainer` registry (one per lifecycle container) + a pure `recommend(ticket)` that maps the ticket's stage (MOD-20 is the rule base) and label signals to role/skill **contracts**, each carrying the resolver's missing-memory (MOD-21). Confidence is categorical (no numeric scores, §8.9); `mapped_tool` is a descriptive label and no secret is handled (DEBT-06/07). A test pins one container per MOD-20 slug (the E17 ripple gate). |
| db-backed registry (E17-T4, v0.2) | **built on the in-stack SQLModel + Alembic** (`skill_containers`/`skill_mappings` collections + `kantaq_core.skills.SkillRegistryService`) | Apache-2.0 (ours) | Category: db-backed config registry. Same outcome as MOD-19 memory: the registry is two relational collections folded into the existing schema (D-07 parity, one metadata, both dialects) — adopting a "skill registry" product would replace the protocol. No external dependency. **DEBT-06 resolved = descriptive** (a `skill_mapping` is a label/pointer the human acts on, not an executable command; `connection` carries no secret → **DEBT-07 moot**). The CRUD service mirrors `MemoryService` but is deliberately **sink-less**: the registry is OFF the sync allowlist in v0.2 (architecture §6.1 "backend registry"; cross-replica sync deferred), so writes are local + audited, never emitted. The 29 hardcoded containers migrate into the registry behind the same contract (a pin asserts the DB slug set == `reco.CONTAINERS`); the pure `recommend()` engine is unchanged in T4 (its DB-read wiring is E17-T5). |

### E08 / MOD-18 security & injection defense (v0.1)

| Need | Chosen | License | Notes |
|---|---|---|---|
| Injection-defense controls | **built from scratch** (`kantaq_mcp.security` + the MOD-08 gateway) | Apache-2.0 (ours) | Golden-rule survey (E08-T0, 2026-06) of published MCP / LLM injection-defense guidance — Anthropic's MCP security guidance and the **OWASP LLM Top 10 (LLM01 prompt injection)** layered-defense pattern: separate data from instructions, least privilege, human-in-the-loop, bound the blast radius. We **implement the server-side controls of that pattern** (not a library): untrusted-content fencing (`tag_untrusted`), the fixed per-session tool allowlist + grant-scoped capabilities (least privilege), propose-first for risky writes (human-in-the-loop), rate limits, loopback-only + origin guards, and the prompt-injection regression **corpus as a CI gate** across every read tool. No OSS clears the bar for this server-side contract: prompt-injection "guard"/classifier libraries (rebuff, llm-guard) are **model-side** detectors — the product runs no model, so they are out of scope. The OWASP layered pattern is claimed honestly: applied, not a named library. |
| External-MCP allowlist (FR-E08-6) | **built from scratch** (`kantaq_mcp.security.ExternalMcpAllowlist`, stdlib `urllib`) | Apache-2.0 (ours) | A small origin-normalizing allowlist: in team mode an external MCP server must be workspace-approved; solo mode is local-trust (D-06). v0.1 federates with no external server (the gateway is loopback-only and proxies to nothing), so the policy gates *configuration* — the seam external-MCP federation will enforce against — with no live traffic path to overclaim. |
| Red-team harness (NFR-E08-1, E08-T5) | **built from scratch** (`kantaq_test_harness.red_team` + `packages/mcp/tests/test_red_team.py`) | Apache-2.0 (ours) | Golden-rule survey (E08-T5, 2026-06) of adversarial-testing OSS: **NVIDIA/garak** (LLM vuln scanner, Apache-2.0, ~4k★), **Azure/PyRIT** (Microsoft, MIT, ~2.5k★), **promptfoo** (MIT, ~6k★ — clears the star bar). All three are **model-side** red-team harnesses: they drive adversarial prompts into a *live LLM target* and grade the model's responses. kantaq runs **no model** — NFR-E08-1 is a *containment* proof of an already-compromised model session against the **gateway boundary**, not a model-jailbreak hunt. None fits that server-side contract (same reasoning as E08-T0). Build-from-scratch on the existing hermetic harness: a declarative `ATTACK_CATALOG` (escalation/exfiltration/bulk-write/queue-skip) driven through the real `Gateway`, each attempt asserted **bounded, denied, and audited**; the manifest is cross-checked against the executed battery (no drift) and the injection corpus replays through the malicious session — a new attack record is a new permanent regression. |

### Sprint 5 release-readiness (E27-T3/T4, E23-T2, E21-T3) — reuse confirmed, no new dependencies

Golden-rule pass run 2026-06-14 for the v0.1 release-readiness bundle. Every task
resolved to **reuse**; no library cleared (or needed to clear) the bar because no
genuinely new capability was introduced.

| Need | Chosen | Notes |
|---|---|---|
| Bundle import (E23-T2) | **built on stdlib + the in-stack codec** (`kantaq_runtime.import_bundle`) | The inverse of the E23-T1 producer: `tarfile`/`gzip`/`json`/`hashlib` (stdlib) + the MOD-17 `decode`/`verify`/`decode_grant` codec + MOD-04 `insert_event`/`refold_entity`. Adopting an import/migration library would replace the protocol. The public `POST /v1/import` endpoint stays v0.2 (DEBT-03); this is the library seam it will call. |
| Round-trip byte-diff (E23-T2) | **stdlib `hashlib`/`json`** | Byte-for-byte NDJSON comparison + SHA-256 content-address checks. No diff library needed. |
| v0.1 gate set + failing fixtures (E27-T3) | **pytest + the existing harness** | The gate manifest re-exercises the real gate primitives (`verify`, `verify_event`/`verify_grant`, `evals.score`, `tag_untrusted`, `HeroFlowTimer`) with seeded regressions. No new tooling. |
| Real timed hero flow (E27-T3) | **the in-stack runtime + MCP gateway + sync engine + `HeroFlowTimer`** | The scripted agent is the existing `FakeMCPClient` over the real gateway; nothing added. |
| Conformance smoke (E27-T4) | **the in-stack protocol + `VerifyingBackend` + `FakeBackend`** | Assembled from shipped seams; no new dependency. |
| Onboarding wizard (E21-T3) | **React + react-router + `ui.ts` + existing endpoints** | A guided wrapper over `POST /v1/projects` + `POST /v1/memory` + the My Agent snippet; same shape as every prior web epic. No new dependency. |

### E11 / MOD-24 client compatibility — Tier-1 (v0.1)

| Need | Chosen | License | Notes |
|---|---|---|---|
| Scripted Tier-1 compatibility client | **official Python MCP SDK client** (`mcp`, already in the stack) via `FakeAgent` (`kantaq_test_harness.compat`) | MIT (SDK) / Apache-2.0 (ours) | Golden-rule run 2026-06-14. The capability is "drive the 8 Tier-1 acceptance tests with the *same client library Claude Code and Cursor embed*." Candidates evaluated: (1) the **official `mcp` SDK** (Anthropic + the MCP org; ADR-0001's MCP choice; ~18k★ across the MCP repos; already a runtime dependency) — its streamable-HTTP client *is* the real Tier-1 client half, so testing against it satisfies the "fake honors the real contract" rule by construction; (2) third-party MCP test harnesses (**mcp-use**, **wong2/mcp-cli**, various `mcp-test-*`) — all below the 5k-star bar and immature; (3) **build a protocol fake from scratch** — rejected, because a hand-rolled client would prove nothing about the contract real clients run under (the explicit MOD-30 anti-pattern). So `FakeAgent` thinly wraps the existing `FakeMCPClient` (real SDK over in-process ASGI) with the grant/role connect headers; zero new dependency. |
| Tier-1 acceptance suite + runner | **pytest + the in-stack runtime/gateway** (`tests/compat`, `scripts/compat_check.py`) | Apache-2.0 (ours) | The 8 tests (PRD §20.4) run the scripted client against the real gateway app + the runtime API (approve/rotate) over one engine + `FakeClock` — the same hermetic-harness pattern as E27's timed hero flow. No new tooling; the runner reuses `pytest.main` to emit the matrix `Pass rate`. |
| Claude Code / Cursor / Codex connection snippets | **config strings, built in-stack** (`/v1/me/agent-snippet`, Settings → My Agent) | Apache-2.0 (ours) | Per-client MCP configs — documented formats, not a library: Claude Code `.mcp.json` (`mcpServers` JSON, `type: http`, inline bearer); Cursor `.cursor/mcp.json` (`mcpServers` JSON, bare `url`, inline bearer); **Codex `~/.codex/config.toml`** (`[mcp_servers.kantaq]` TOML, `bearer_token_env_var` — the token rides an env var, never the file). The generator never round-trips a token (NFR-E06-1); the URL is loopback-asserted; TOML is rendered server-side (no client serializer). No standard claimed beyond MCP's own config convention, which we follow. |

### E05 / MOD-26 offline-sync-conflicts (v0.2 spec finalized at E05-T0)

Golden-rule re-run 2026-06 for the v0.2 offline-first + conflict layer (the spec spike; **no code shipped** this task). Three capability categories evaluated against the bar with current data; **build-from-scratch on the existing signed-event log in all three** — extends the MOD-04 decision ("the event protocol IS the product"; the merge/conflict engine *is* the product, not a dependency).

| Need | Chosen | License | Notes |
|---|---|---|---|
| Conflict-resolution engine (different-scalar auto-merge / same-scalar → `conflict_record` / append-only never conflicts / tombstone idempotent / stale-proposal → `rebase_required`) | **build from scratch** in `kantaq_sync_engine` (the §8.1 decision implemented at the atomic RPC, mirrored in Python for self-host + client preview, cross-checked against a golden `conflict_vectors.json`) | Apache-2.0 (ours) | CRDT category surveyed: **Yjs** (~22k★, MIT), **Automerge** (~6.3k★, MIT), **Loro** (~5.3k★, MIT) all clear the bar; **cr-sqlite** (~3.7k★), **Diamond Types** (~1.8k★), **json-joy** (~1k★), the **RxDB CRDT plugin** (TS, open-core) all fail it. **Every one is the wrong shape** — a CRDT owns its op/document model and converges by *silent automatic merge*, which CRDT-being-locked-out-of-scope (architecture §10), the §8.1 "no silent auto-merge of free-text fields" rule, and the three un-met CRDT-adoption triggers all forbid; none surfaces a scalar `conflict_record` or threads kantaq's Ed25519-signed Events. |
| Offline replica / sync framework (durable offline outbox/inbox + reconcile-on-reconnect) | **build from scratch** on the existing `event_log` (pending rows = the outbox; a new local `sync_state` column adds terminal states; the existing crash-safe `pull()` is the inbox) | Apache-2.0 (ours) | Frameworks surveyed: **ElectricSQL** (~10k★, Apache-2.0 — Postgres→client replication, own write model, already rejected for MOD-04), **RxDB** (~23k★, Apache-2.0 core but premium/SSPL plugins; TS, owns its model), **PowerSync** (sub-5k + FSL server), **WatermelonDB** (stale, RN/JS-only), **Triplit** (sub-5k **and** AGPL — banned), **Jazz/Zero/SQLSync/Y-Sweet/Evolu** (sub-5k and/or CRDT). None clears the bar **and** matches the append-only-signed-event shape. (Keep **Turso/libSQL embedded replicas** on the radar as a future *storage* primitive under the SQLite WAL replica — not the conflict/sync engine.) |
| Durable transactional-outbox / idempotency-key layer | **build from scratch** (`(actor_id, actor_seq)` dedup + `event_id` ULID, both already present; add outbox terminal-states) | Apache-2.0 (ours) | The bar-clearing options (**Celery** 28.6k★/BSD, **Hatchet** 7.2k★/MIT, **PGMQ** 4.9k★) are server-side broker queues assuming an always-online substrate — the opposite of a local-first embedded-SQLite outbox; **procrastinate** (~1.3k★) and **Dramatiq** (5.3k★ but LGPL/GPL) fail bar/license; the true outbox/idempotency micro-libs are far sub-5k and unmaintained. kantaq's durability + idempotency are intrinsic to the protocol. |

## Consequences

- Two toolchains in CI (Python + web). Keep total CI **under 10 minutes**
  (RISK-05) via dependency caching and parallel jobs.
- FastAPI emits OpenAPI; CI regenerates the TS client and fails on drift (D-08,
  lands with E18).
- One schema definition generates both SQLite and Postgres DDL (D-07, lands with
  E02).
